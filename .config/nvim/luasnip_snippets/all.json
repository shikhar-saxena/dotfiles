{
  "date": {
    "prefix": "date",
    "description": "Date",
    "body": "${CURRENT_MONTH_NAME} ${CURRENT_DATE}, ${CURRENT_YEAR}"
  },
  "sdate": {
    "prefix": "sdate",
    "description": "Small Date",
    "body": "${CURRENT_YEAR}-${CURRENT_MONTH}-${CURRENT_DATE}"
  }
}
